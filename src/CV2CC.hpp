#include "OrangeLine.hpp"

enum jsonIds
{
	STYLE_JSON,
	FLUSH_ON_START_JSON,
	NUM_JSONS
};

enum ParamIds
{
	FLUSH_PARAM,
	NUM_PARAMS
};

enum InputIds
{
	CC_INPUT,
	CC_INPUT_LAST = CC_INPUT + 8 - 1,
	FORCE_INPUT,
	NUM_INPUTS
};

enum OutputIds
{
	NUM_OUTPUTS
};

enum LightIds
{
	NUM_LIGHTS
};
