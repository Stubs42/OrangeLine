#include "OrangeLine.hpp"

enum jsonIds
{
	STYLE_JSON,
	CURVE_UNI_JSON,
	
	NUM_JSONS
};

enum ParamIds
{
	NUM_PARAMS
};

enum InputIds
{
	GATE_INPUT,
	VEL_INPUT,
	SHAPE_INPUT,
	ATTEN_INPUT,
	NUM_INPUTS
};

enum OutputIds
{
	HEATOFF_OUTPUT,
	HEATSCL_OUTPUT,
	VEL_OUTPUT,
	NUM_OUTPUTS
};

enum LightIds
{
	NUM_LIGHTS
};
