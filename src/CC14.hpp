#include "OrangeLine.hpp"

enum jsonIds
{
	STYLE_JSON,
	NUM_JSONS
};

enum ParamIds
{
	NUM_PARAMS
};

enum InputIds
{
	MSB_INPUT,
	LSB_INPUT,
	CV_INPUT,
	NUM_INPUTS
};

enum OutputIds
{
	CV_OUTPUT,
	MSB_OUTPUT,
	LSB_OUTPUT,
	NUM_OUTPUTS
};

enum LightIds
{
	NUM_LIGHTS
};
